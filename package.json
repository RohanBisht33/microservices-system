{
  "name": "microservices-system",
  "version": "1.0.0",
  "private": true,
  "description": "Microservices system with User Service, Notification Service, API Gateway, and NATS JetStream",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "jest --config jest.config.json",
    "gen-certs": "bash scripts/gen-certs.sh"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
